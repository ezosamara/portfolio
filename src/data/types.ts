// ============================================================
// Type bridge — generated files import from here.
// Re-exports the canonical types from src/types/index.ts
// so that projects.generated.ts and timeline.generated.ts
// use the same interfaces the rest of the app expects.
// ============================================================

export type {
  Project,
  ProjectDetail,
  ProjectMetric,
  ProjectCategory,
  TimelineItem,
  TimelineType,
  LocalizedText,
  Lang,
  Service,
  Stat,
} from '../types';
